# 校园失物招领与位置追踪系统 - 数据库文档

> **最后更新**：2026-06-03
>
> **数据库**：MySQL
>
> **数据库名**：compus
>
> **连接信息**：
>
> - 主机：localhost
> - 端口：3306
> - 用户名：mapuser
> - 密码：123456
> - 字符集：utf8mb4

---

## 一、数据库表总览

| 序号 | 表名                  | 说明            |
| :--- | :-------------------- | :-------------- |
| 1    | admin                 | 管理员表        |
| 2    | appointment           | 预约表          |
| 3    | audit                 | 统一审核表      |
| 4    | category              | 物品分类表      |
| 5    | claim_form            | 认领表单表      |
| 6    | comment_likes         | 评论点赞表      |
| 7    | community_comments    | 社区评论表      |
| 8    | community_post_images | 帖子图片表      |
| 9    | community_posts       | 社区帖子表      |
| 10   | item_campus           | 物品校区关联表  |
| 11   | location              | 地理位置表      |
| 12   | lost_item             | 失物招领物品表  |
| 13   | map_config            | 校区/地图配置表 |
| 14   | messages              | 消息通知表      |
| 15   | nav_nodes             | 导航节点表      |
| 16   | notifications         | 社区通知表      |
| 17   | permission_definition | 权限定义表      |
| 18   | post_likes            | 帖子点赞表      |
| 19   | post_tag              | 帖子标签关联表  |
| 20   | privilege_request     | 权限申请表      |
| 21   | repair                | 报修表          |
| 22   | repair_campus         | 报修校区关联表  |
| 23   | repair_category       | 报修分类表      |
| 24   | return_form           | 归还表单表      |
| 25   | system_logs           | 系统日志表      |
| 26   | user                  | 用户表          |
| 27   | user_follow           | 用户关注表      |
| 28   | user_verify           | 用户实名认证表  |

---

## 二、详细表结构

### 2.1 统一审核表（audit）

**功能描述**：存储所有类型审核的统一管理表

| 字段名         | 类型         | 约束     | 默认值            | 说明                                                                 |
| :------------- | :----------- | :------- | :---------------- | :------------------------------------------------------------------- |
| audit_id       | int          | NOT NULL |                   | 审核记录ID，主键                                                    |
| target_type    | varchar(50)  | NOT NULL |                   | 审核目标类型: lost_item, claim, claim_form, return_form, user_verify, appointment, privilege_request |
| target_id      | int          | NOT NULL |                   | 关联目标ID                                                          |
| requester_id   | int          |          |                   | 申请人ID                                                            |
| status         | varchar(20)  | NOT NULL | pending           | 审核状态: pending, approved, rejected                               |
| audit_by       | int          |          |                   | 审核人ID(管理员)                                                    |
| audit_time     | datetime     |          |                   | 审核时间                                                            |
| audit_remark   | varchar(500) |          |                   | 审核备注                                                            |
| create_time    | datetime     |          | CURRENT_TIMESTAMP | 创建时间                                                            |
| update_time    | datetime     |          | CURRENT_TIMESTAMP | 更新时间                                                            |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS audit (
    audit_id INT AUTO_INCREMENT PRIMARY KEY,
    target_type VARCHAR(50) NOT NULL COMMENT '审核目标类型: lost_item, claim, claim_form, return_form, user_verify, appointment, privilege_request',
    target_id INT NOT NULL COMMENT '关联目标ID',
    requester_id INT COMMENT '申请人ID',
    status VARCHAR(20) NOT NULL DEFAULT 'pending' COMMENT '审核状态: pending, approved, rejected',
    audit_by INT COMMENT '审核人ID(管理员)',
    audit_time DATETIME COMMENT '审核时间',
    audit_remark VARCHAR(500) COMMENT '审核备注',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_target_type (target_type),
    INDEX idx_target_id (target_id),
    INDEX idx_status (status),
    INDEX idx_requester (requester_id),
    INDEX idx_audit_by (audit_by)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='统一审核表';
```

---

### 2.2 管理员表（admin）

**功能描述**：存储管理员信息

| 字段名          | 类型         | 约束     | 默认值            | 说明 |
| :-------------- | :----------- | :------- | :---------------- | :--- |
| admin_id        | int          | NOT NULL |                   |      |
| username        | varchar(50)  | NOT NULL |                   |      |
| password_hash   | varchar(255) | NOT NULL |                   |      |
| email           | varchar(100) |          |                   |      |
| phone           | varchar(20)  |          |                   |      |
| create_time     | datetime     |          | CURRENT_TIMESTAMP |      |
| last_login      | datetime     |          |                   |      |
| role            | varchar(20)  |          | secondary_admin   |      |
| permissions     | text         |          |                   |      |
| department      | varchar(100) |          |                   |      |
| permission_type | varchar(20)  |          | general           |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS admin (
    admin_id int NOT NULL,
    username varchar(50) NOT NULL,
    password_hash varchar(255) NOT NULL,
    email varchar(100),
    phone varchar(20),
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    last_login datetime,
    role varchar(20) DEFAULT secondary_admin,
    permissions text,
    department varchar(100),
    permission_type varchar(20) DEFAULT general,
    PRIMARY KEY (admin_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.2 预约表（appointment）

**功能描述**：存储预约信息

| 字段名           | 类型                                                          | 约束     | 默认值            | 说明 |
| :--------------- | :------------------------------------------------------------ | :------- | :---------------- | :--- |
| appointment_id   | int                                                           | NOT NULL |                   |      |
| item_id          | int                                                           | NOT NULL |                   |      |
| user_id          | int                                                           | NOT NULL |                   |      |
| appointment_time | datetime                                                      | NOT NULL |                   |      |
| location         | varchar(255)                                                  | NOT NULL |                   |      |
| longitude        | decimal(10,7)                                                 |          |                   |      |
| latitude         | decimal(10,7)                                                 |          |                   |      |
| contact_name     | varchar(100)                                                  | NOT NULL |                   |      |
| contact_phone    | varchar(20)                                                   | NOT NULL |                   |      |
| note             | text                                                          |          |                   |      |
| status           | enum('pending','approved','rejected','completed','cancelled') |          | pending           |      |
| reviewer_id      | int                                                           |          |                   |      |
| review_time      | datetime                                                      |          |                   |      |
| review_note      | text                                                          |          |                   |      |
| create_time      | datetime                                                      |          | CURRENT_TIMESTAMP |      |
| update_time      | datetime                                                      |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS appointment (
    appointment_id int NOT NULL,
    item_id int NOT NULL,
    user_id int NOT NULL,
    appointment_time datetime NOT NULL,
    location varchar(255) NOT NULL,
    longitude decimal(10,7),
    latitude decimal(10,7),
    contact_name varchar(100) NOT NULL,
    contact_phone varchar(20) NOT NULL,
    note text,
    status enum('pending','approved','rejected','completed','cancelled') DEFAULT pending,
    reviewer_id int,
    review_time datetime,
    review_note text,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    update_time datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (appointment_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.3 物品分类表（category）

**功能描述**：存储物品分类信息

| 字段名      | 类型         | 约束     | 默认值            | 说明 |
| :---------- | :----------- | :------- | :---------------- | :--- |
| id          | int          | NOT NULL |                   |      |
| name        | varchar(50)  | NOT NULL |                   |      |
| description | varchar(200) |          |                   |      |
| sort_order  | int          |          | 0                 |      |
| is_active   | tinyint(1)   |          | 1                 |      |
| created_at  | datetime     |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS category (
    id int NOT NULL,
    name varchar(50) NOT NULL,
    description varchar(200),
    sort_order int DEFAULT 0,
    is_active tinyint(1) DEFAULT 1,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.4 认领表单表（claim_form）

**功能描述**：存储认领单信息

| 字段名           | 类型         | 约束     | 默认值            | 说明 |
| :--------------- | :----------- | :------- | :---------------- | :--- |
| claim_id         | int          | NOT NULL |                   |      |
| item_id          | int          |          |                   |      |
| applicant_name   | varchar(100) |          |                   |      |
| applicant_phone  | varchar(50)  |          |                   |      |
| claim_reason     | text         | NOT NULL |                   |      |
| item_description | text         | NOT NULL |                   |      |
| user_id          | int          |          |                   |      |
| proof_images     | longtext     |          |                   |      |
| status           | smallint     |          | 0                 |      |
| create_time      | datetime     |          | CURRENT_TIMESTAMP |      |
| applicant_email  | varchar(100) |          |                   |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS claim_form (
    claim_id int NOT NULL,
    item_id int,
    applicant_name varchar(100),
    applicant_phone varchar(50),
    claim_reason text NOT NULL,
    item_description text NOT NULL,
    user_id int,
    proof_images longtext,
    status smallint DEFAULT 0,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    applicant_email varchar(100),
    PRIMARY KEY (claim_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.5 评论点赞表（comment_likes）

**功能描述**：存储评论点赞信息

| 字段名     | 类型     | 约束     | 默认值            | 说明 |
| :--------- | :------- | :------- | :---------------- | :--- |
| like_id    | int      | NOT NULL |                   |      |
| comment_id | int      | NOT NULL |                   |      |
| user_id    | int      | NOT NULL |                   |      |
| created_at | datetime |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS comment_likes (
    like_id int NOT NULL,
    comment_id int NOT NULL,
    user_id int NOT NULL,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (like_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.6 社区评论表（community_comments）

**功能描述**：存储社区评论信息

| 字段名     | 类型     | 约束     | 默认值            | 说明 |
| :--------- | :------- | :------- | :---------------- | :--- |
| comment_id | int      | NOT NULL |                   |      |
| post_id    | int      | NOT NULL |                   |      |
| user_id    | int      | NOT NULL |                   |      |
| parent_id  | int      |          |                   |      |
| content    | text     | NOT NULL |                   |      |
| created_at | datetime |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS community_comments (
    comment_id int NOT NULL,
    post_id int NOT NULL,
    user_id int NOT NULL,
    parent_id int,
    content text NOT NULL,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (comment_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.7 帖子图片表（community_post_images）

**功能描述**：存储帖子图片信息

| 字段名      | 类型        | 约束     | 默认值            | 说明 |
| :---------- | :---------- | :------- | :---------------- | :--- |
| image_id    | int         | NOT NULL |                   |      |
| post_id     | int         | NOT NULL |                   |      |
| image_data  | text        |          |                   |      |
| image_type  | varchar(50) |          |                   |      |
| sort_order  | int         |          | 0                 |      |
| create_time | datetime    |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS community_post_images (
    image_id int NOT NULL,
    post_id int NOT NULL,
    image_data text,
    image_type varchar(50),
    sort_order int DEFAULT 0,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (image_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.8 社区帖子表（community_posts）

**功能描述**：存储社区帖子信息

| 字段名         | 类型         | 约束     | 默认值            | 说明 |
| :------------- | :----------- | :------- | :---------------- | :--- |
| post_id        | int          | NOT NULL |                   |      |
| user_id        | int          | NOT NULL |                   |      |
| title          | varchar(200) | NOT NULL |                   |      |
| content        | text         | NOT NULL |                   |      |
| images         | longtext     |          |                   |      |
| created_at     | datetime     |          | CURRENT_TIMESTAMP |      |
| is_commentable | tinyint(1)   |          | 1                 |      |
| is_public      | tinyint(1)   |          | 1                 |      |
| updated_at     | datetime     |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS community_posts (
    post_id int NOT NULL,
    user_id int NOT NULL,
    title varchar(200) NOT NULL,
    content text NOT NULL,
    images longtext,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    is_commentable tinyint(1) DEFAULT 1,
    is_public tinyint(1) DEFAULT 1,
    updated_at datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (post_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.9 物品校区关联表（item_campus）

**功能描述**：存储物品校区关联信息

| 字段名      | 类型     | 约束     | 默认值            | 说明 |
| :---------- | :------- | :------- | :---------------- | :--- |
| id          | int      | NOT NULL |                   |      |
| item_id     | int      | NOT NULL |                   |      |
| campus_id   | int      | NOT NULL |                   |      |
| create_time | datetime |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS item_campus (
    id int NOT NULL,
    item_id int NOT NULL,
    campus_id int NOT NULL,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.10 地理位置表（location）

**功能描述**：存储地理位置信息

| 字段名      | 类型          | 约束     | 默认值 | 说明 |
| :---------- | :------------ | :------- | :----- | :--- |
| location_id | int           | NOT NULL |        |      |
| name        | varchar(200)  | NOT NULL |        |      |
| latitude    | decimal(10,8) | NOT NULL |        |      |
| longitude   | decimal(11,8) | NOT NULL |        |      |
| detail      | varchar(500)  |          |        |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS location (
    location_id int NOT NULL,
    name varchar(200) NOT NULL,
    latitude decimal(10,8) NOT NULL,
    longitude decimal(11,8) NOT NULL,
    detail varchar(500),
    PRIMARY KEY (location_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.12 失物招领物品表（lost_item）

**功能描述**：存储失物招领物品信息（审核信息统一存储在audit表中）

| 字段名       | 类型         | 约束     | 默认值            | 说明 |
| :----------- | :----------- | :------- | :---------------- | :--- |
| item_id      | int          | NOT NULL |                   |      |
| title        | varchar(100) | NOT NULL |                   |      |
| description  | text         |          |                   |      |
| type         | tinyint(1)   | NOT NULL |                   |      |
| category_id  | int          |          |                   |      |
| status       | tinyint(1)   |          | 0                 |      |
| image_urls   | json         |          |                   |      |
| publisher_id | int          | NOT NULL |                   |      |
| location_id  | int          |          |                   |      |
| create_time  | datetime     |          | CURRENT_TIMESTAMP |      |
| update_time  | datetime     |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS lost_item (
    item_id int NOT NULL,
    title varchar(100) NOT NULL,
    description text,
    type tinyint(1) NOT NULL,
    category_id int,
    status tinyint(1) DEFAULT 0,
    image_urls json,
    publisher_id int NOT NULL,
    location_id int,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    update_time datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (item_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.12 校区/地图配置表（map_config）

**功能描述**：存储校区/地图配置信息

| 字段名     | 类型         | 约束     | 默认值 | 说明 |
| :--------- | :----------- | :------- | :----- | :--- |
| id         | int          | NOT NULL |        |      |
| name       | varchar(100) |          |        |      |
| center_lng | double       |          |        |      |
| center_lat | double       |          |        |      |
| sw_lng     | double       |          |        |      |
| sw_lat     | double       |          |        |      |
| ne_lng     | double       |          |        |      |
| ne_lat     | double       |          |        |      |
| zoom       | int          |          | 18     |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS map_config (
    id int NOT NULL,
    name varchar(100),
    center_lng double,
    center_lat double,
    sw_lng double,
    sw_lat double,
    ne_lng double,
    ne_lat double,
    zoom int DEFAULT 18,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.13 消息通知表（messages）

**功能描述**：存储消息通知信息

| 字段名       | 类型         | 约束     | 默认值            | 说明 |
| :----------- | :----------- | :------- | :---------------- | :--- |
| message_id   | int          | NOT NULL |                   |      |
| user_id      | int          | NOT NULL |                   |      |
| title        | varchar(255) | NOT NULL |                   |      |
| content      | text         | NOT NULL |                   |      |
| message_type | varchar(20)  | NOT NULL | system            |      |
| is_read      | tinyint(1)   |          | 0                 |      |
| create_time  | datetime     |          | CURRENT_TIMESTAMP |      |
| item_id      | int          |          |                   |      |
| is_deleted   | tinyint(1)   |          | 0                 |      |
| delete_time  | datetime     |          |                   |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS messages (
    message_id int NOT NULL,
    user_id int NOT NULL,
    title varchar(255) NOT NULL,
    content text NOT NULL,
    message_type varchar(20) NOT NULL DEFAULT system,
    is_read tinyint(1) DEFAULT 0,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    item_id int,
    is_deleted tinyint(1) DEFAULT 0,
    delete_time datetime,
    PRIMARY KEY (message_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.14 导航节点表（nav_nodes）

**功能描述**：存储导航节点信息

| 字段名     | 类型                                                          | 约束     | 默认值            | 说明 |
| :--------- | :------------------------------------------------------------ | :------- | :---------------- | :--- |
| node_id    | int                                                           | NOT NULL |                   |      |
| name       | varchar(100)                                                  |          |                   |      |
| lng        | decimal(15,10)                                                | NOT NULL |                   |      |
| lat        | decimal(15,10)                                                | NOT NULL |                   |      |
| type       | enum('intersection','building','entrance','landmark','other') |          | intersection      |      |
| campus_id  | int                                                           |          |                   |      |
| created_at | timestamp                                                     |          | CURRENT_TIMESTAMP |      |
| updated_at | timestamp                                                     |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS nav_nodes (
    node_id int NOT NULL,
    name varchar(100),
    lng decimal(15,10) NOT NULL,
    lat decimal(15,10) NOT NULL,
    type enum('intersection','building','entrance','landmark','other') DEFAULT intersection,
    campus_id int,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP,
    updated_at timestamp DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (node_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.15 社区通知表（notifications）

**功能描述**：存储社区通知信息

| 字段名          | 类型        | 约束     | 默认值            | 说明 |
| :-------------- | :---------- | :------- | :---------------- | :--- |
| notification_id | int         | NOT NULL |                   |      |
| user_id         | int         | NOT NULL |                   |      |
| type            | varchar(50) | NOT NULL |                   |      |
| content         | text        | NOT NULL |                   |      |
| post_id         | int         |          |                   |      |
| comment_id      | int         |          |                   |      |
| is_read         | tinyint     |          | 0                 |      |
| created_at      | datetime    |          | CURRENT_TIMESTAMP |      |
| trigger_user_id | int         |          |                   |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS notifications (
    notification_id int NOT NULL,
    user_id int NOT NULL,
    type varchar(50) NOT NULL,
    content text NOT NULL,
    post_id int,
    comment_id int,
    is_read tinyint DEFAULT 0,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    trigger_user_id int,
    PRIMARY KEY (notification_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.16 权限定义表（permission_definition）

**功能描述**：存储权限定义信息

| 字段名          | 类型         | 约束     | 默认值            | 说明 |
| :-------------- | :----------- | :------- | :---------------- | :--- |
| permission_id   | int          | NOT NULL |                   |      |
| permission_key  | varchar(50)  | NOT NULL |                   |      |
| permission_name | varchar(100) | NOT NULL |                   |      |
| description     | text         |          |                   |      |
| category        | varchar(50)  |          |                   |      |
| create_time     | datetime     |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS permission_definition (
    permission_id int NOT NULL,
    permission_key varchar(50) NOT NULL,
    permission_name varchar(100) NOT NULL,
    description text,
    category varchar(50),
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (permission_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.17 帖子点赞表（post_likes）

**功能描述**：存储帖子点赞信息

| 字段名     | 类型     | 约束     | 默认值            | 说明 |
| :--------- | :------- | :------- | :---------------- | :--- |
| like_id    | int      | NOT NULL |                   |      |
| post_id    | int      | NOT NULL |                   |      |
| user_id    | int      | NOT NULL |                   |      |
| created_at | datetime |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS post_likes (
    like_id int NOT NULL,
    post_id int NOT NULL,
    user_id int NOT NULL,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (like_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.18 帖子标签关联表（post_tag）

**功能描述**：存储帖子标签关联信息

| 字段名     | 类型        | 约束     | 默认值            | 说明 |
| :--------- | :---------- | :------- | :---------------- | :--- |
| id         | int         | NOT NULL |                   |      |
| post_id    | int         | NOT NULL |                   |      |
| tag_name   | varchar(50) | NOT NULL |                   |      |
| created_at | datetime    |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS post_tag (
    id int NOT NULL,
    post_id int NOT NULL,
    tag_name varchar(50) NOT NULL,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.19 权限申请表（privilege_request）

**功能描述**：存储权限申请信息

| 字段名                | 类型        | 约束     | 默认值            | 说明 |
| :-------------------- | :---------- | :------- | :---------------- | :--- |
| request_id            | int         | NOT NULL |                   |      |
| admin_id              | int         | NOT NULL |                   |      |
| reason                | text        |          |                   |      |
| status                | varchar(20) |          | pending           |      |
| create_time           | datetime    |          | CURRENT_TIMESTAMP |      |
| review_time           | datetime    |          |                   |      |
| reviewer_id           | int         |          |                   |      |
| review_note           | text        |          |                   |      |
| requested_permissions | text        |          |                   |      |
| target_admin_id       | int         |          |                   |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS privilege_request (
    request_id int NOT NULL,
    admin_id int NOT NULL,
    reason text,
    status varchar(20) DEFAULT pending,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    review_time datetime,
    reviewer_id int,
    review_note text,
    requested_permissions text,
    target_admin_id int,
    PRIMARY KEY (request_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.20 报修表（repair）

**功能描述**：存储报修信息

| 字段名             | 类型         | 约束     | 默认值            | 说明 |
| :----------------- | :----------- | :------- | :---------------- | :--- |
| repair_id          | int          | NOT NULL |                   |      |
| title              | varchar(100) | NOT NULL |                   |      |
| description        | text         |          |                   |      |
| repair_category_id | int          |          |                   |      |
| location_id        | int          |          |                   |      |
| reporter_id        | int          | NOT NULL |                   |      |
| images             | text         |          |                   |      |
| status             | smallint     |          | 0                 |      |
| priority           | smallint     |          | 1                 |      |
| assignee_id        | int          |          |                   |      |
| process_note       | text         |          |                   |      |
| create_time        | timestamp    |          | CURRENT_TIMESTAMP |      |
| update_time        | timestamp    |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS repair (
    repair_id int NOT NULL,
    title varchar(100) NOT NULL,
    description text,
    repair_category_id int,
    location_id int,
    reporter_id int NOT NULL,
    images text,
    status smallint DEFAULT 0,
    priority smallint DEFAULT 1,
    assignee_id int,
    process_note text,
    create_time timestamp DEFAULT CURRENT_TIMESTAMP,
    update_time timestamp DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (repair_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.21 报修校区关联表（repair_campus）

**功能描述**：存储报修校区关联信息

| 字段名      | 类型     | 约束     | 默认值            | 说明 |
| :---------- | :------- | :------- | :---------------- | :--- |
| id          | int      | NOT NULL |                   |      |
| repair_id   | int      | NOT NULL |                   |      |
| campus_id   | int      | NOT NULL |                   |      |
| create_time | datetime |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS repair_campus (
    id int NOT NULL,
    repair_id int NOT NULL,
    campus_id int NOT NULL,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.22 报修分类表（repair_category）

**功能描述**：存储报修分类信息

| 字段名             | 类型         | 约束     | 默认值 | 说明 |
| :----------------- | :----------- | :------- | :----- | :--- |
| repair_category_id | int          | NOT NULL |        |      |
| name               | varchar(50)  | NOT NULL |        |      |
| icon               | varchar(100) |          |        |      |
| description        | varchar(200) |          |        |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS repair_category (
    repair_category_id int NOT NULL,
    name varchar(50) NOT NULL,
    icon varchar(100),
    description varchar(200),
    PRIMARY KEY (repair_category_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.23 归还表单表（return_form）

**功能描述**：存储归还单信息

| 字段名           | 类型         | 约束     | 默认值            | 说明 |
| :--------------- | :----------- | :------- | :---------------- | :--- |
| return_id        | int          | NOT NULL |                   |      |
| item_id          | int          |          |                   |      |
| applicant_name   | varchar(100) |          |                   |      |
| applicant_phone  | varchar(50)  | NOT NULL |                   |      |
| return_reason    | text         | NOT NULL |                   |      |
| item_description | text         | NOT NULL |                   |      |
| user_id          | int          |          |                   |      |
| proof_images     | longtext     |          |                   |      |
| status           | smallint     |          | 0                 |      |
| create_time      | datetime     |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS return_form (
    return_id int NOT NULL,
    item_id int,
    applicant_name varchar(100),
    applicant_phone varchar(50) NOT NULL,
    return_reason text NOT NULL,
    item_description text NOT NULL,
    user_id int,
    proof_images longtext,
    status smallint DEFAULT 0,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (return_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.24 系统日志表（system_logs）

**功能描述**：存储系统日志信息

| 字段名      | 类型        | 约束     | 默认值            | 说明 |
| :---------- | :---------- | :------- | :---------------- | :--- |
| id          | int         | NOT NULL |                   |      |
| user_id     | int         |          |                   |      |
| action      | varchar(50) | NOT NULL |                   |      |
| target_type | varchar(50) |          |                   |      |
| target_id   | varchar(50) |          |                   |      |
| detail      | text        |          |                   |      |
| ip_address  | varchar(50) |          |                   |      |
| created_at  | datetime    |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS system_logs (
    id int NOT NULL,
    user_id int,
    action varchar(50) NOT NULL,
    target_type varchar(50),
    target_id varchar(50),
    detail text,
    ip_address varchar(50),
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.25 用户表（user）

**功能描述**：存储用户信息

| 字段名        | 类型         | 约束     | 默认值            | 说明 |
| :------------ | :----------- | :------- | :---------------- | :--- |
| user_id       | int          | NOT NULL |                   |      |
| username      | varchar(50)  | NOT NULL |                   |      |
| password_hash | varchar(255) | NOT NULL |                   |      |
| email         | varchar(100) |          |                   |      |
| phone         | varchar(20)  |          |                   |      |
| avatar_url    | varchar(500) |          |                   |      |
| create_time   | datetime     |          | CURRENT_TIMESTAMP |      |
| signature     | varchar(255) |          |                   |      |
| bio           | text         |          |                   |      |
| bg_image      | varchar(500) |          |                   |      |
| role          | varchar(20)  |          | student           |      |
| status        | varchar(20)  |          | normal            |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS user (
    user_id int NOT NULL,
    username varchar(50) NOT NULL,
    password_hash varchar(255) NOT NULL,
    email varchar(100),
    phone varchar(20),
    avatar_url varchar(500),
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    signature varchar(255),
    bio text,
    bg_image varchar(500),
    role varchar(20) DEFAULT student,
    status varchar(20) DEFAULT normal,
    PRIMARY KEY (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.26 用户关注表（user_follow）

**功能描述**：存储用户关注信息

| 字段名      | 类型     | 约束     | 默认值            | 说明 |
| :---------- | :------- | :------- | :---------------- | :--- |
| id          | int      | NOT NULL |                   |      |
| follower_id | int      | NOT NULL |                   |      |
| followee_id | int      | NOT NULL |                   |      |
| created_at  | datetime |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS user_follow (
    id int NOT NULL,
    follower_id int NOT NULL,
    followee_id int NOT NULL,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 2.27 用户实名认证表（user_verify）

**功能描述**：存储用户实名认证信息

| 字段名        | 类型         | 约束     | 默认值            | 说明 |
| :------------ | :----------- | :------- | :---------------- | :--- |
| verify_id     | int          | NOT NULL |                   |      |
| user_id       | int          | NOT NULL |                   |      |
| identity      | varchar(50)  | NOT NULL |                   |      |
| real_name     | varchar(100) | NOT NULL |                   |      |
| id_card       | varchar(18)  | NOT NULL |                   |      |
| student_id    | varchar(50)  |          |                   |      |
| school        | varchar(200) |          |                   |      |
| phone         | varchar(20)  |          |                   |      |
| email         | varchar(100) |          |                   |      |
| id_card_front | longtext     | NOT NULL |                   |      |
| id_card_back  | longtext     | NOT NULL |                   |      |
| status        | tinyint      |          | 0                 |      |
| create_time   | datetime     |          | CURRENT_TIMESTAMP |      |
| update_time   | datetime     |          | CURRENT_TIMESTAMP |      |

**建表语句**：

```sql
CREATE TABLE IF NOT EXISTS user_verify (
    verify_id int NOT NULL,
    user_id int NOT NULL,
    identity varchar(50) NOT NULL,
    real_name varchar(100) NOT NULL,
    id_card varchar(18) NOT NULL,
    student_id varchar(50),
    school varchar(200),
    phone varchar(20),
    email varchar(100),
    id_card_front longtext NOT NULL,
    id_card_back longtext NOT NULL,
    status tinyint DEFAULT 0,
    create_time datetime DEFAULT CURRENT_TIMESTAMP,
    update_time datetime DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (verify_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---
